cd src/main/frontend

1 gptService에 한글 입력하면 utf-8 코덱이 없어 json 변환이 안됨

2 package.json에서 설치된 라이브러리 확인 가능

3 onChange는 html에선 마우스 벗어나면 호출 리액트에선 값이 변경되면 호출 

4 버튼을 누르면 form 태그의 value를 조작하는 법이 없을까?

5 쿼리셀렉터를 쓰면 직접 DOM을 조작하기에 대신 Ref 사용

6 현재 마운트시 어느 컴포넌트가 초기화되고 어느 함수가 작동하는지 파악 중

7 중간에 깃을 변경하려고 git 폴더를 지웠는데 그래도 기존 정보가 남아있음

8. mui 사용시 div는 블록이고 p는 인라인이기 때문에 순서 워닝이 발생함